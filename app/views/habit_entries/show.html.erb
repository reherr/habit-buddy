<p style="color: green"><%= notice %></p>

<%= render @habit_entry %>

<div>
  <%= link_to "Edit this habit entry", edit_habit_entry_path(@habit_entry) %> |
  <%= link_to "Back to habit", habit_path(@habit) %>

  <%= button_to "Destroy this habit entry", @habit_entry, method: :delete %>
</div>
