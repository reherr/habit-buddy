<div class="container my-4">
  <div class="row justify-content-center">
    <div class="col-lg-8">
      <section class="card shadow-sm" aria-labelledby="edit-habit-entry-title">
        <div class="card-body">
          <h1 id="edit-habit-entry-title" class="card-title mb-4">Edit Habit Entry</h1>

          <%= render "form", habit_entry: @habit_entry %>

          <nav class="mt-4" aria-label="Edit habit entry navigation">
            <%= link_to habit_path(@habit_entry.habit), class: 'btn btn-outline-secondary', aria: { label: "Return to " + @habit_entry.habit.name } do %>
              <i class="fas fa-arrow-left" aria-hidden="true"></i> Back to Habit
            <% end %>
          </nav>
        </div>
      </section>
    </div>
  </div>
</div>

